# 台股波段雷達：實作與驗證範圍

本次在原 stock-watchlist 中擴充，沿用既有 Telegram Bot、GitHub Pages、行情來源與美股報告。
預設 **shadow（影子模式）**：計算並呈現研究結果，只有原本的收盤報告通知；不推送新的波段交易訊號。
不宣稱提高勝率，仍需以未參與參數調整的資料及前瞻紀錄驗證。

## 實際資料流程

1. 原有台股全市場掃描 → 交易日／日期／有限數值／至少21筆有效歷史檢查。
2. 有效覆蓋率低於90%直接失敗；保留前次正式快取，保存品質诊斷。
3. 使用有效樣本的20日報酬中位數與市場廣度作初步市場／產業代理基準。它們不是加權／櫃買指數，也尚未排除所有公司行動對初篩的影響。
4. 依流動性與接近20日線程度挑選20檔深入分析，另保留 `watchlist.json` 指定股與未結束計畫。不是只有舊的異動清單，也不是宣稱全市場每檔都完成基本面研究。
5. 候選股以既有 yfinance 套件取得一年還原日K，還原到最新實際價基準，與主來源收盤價核對；來源價差超過1%、近60交易日缺漏、歷史不足或未完成週K均不得通過。
6. FinMind 逐檔取得月營收、損益表、外資與投信買賣；沿用 `FINMIND_TOKEN`，不需要新的 AI 密鑰。取不到則待確認。
7. 五層檢查：市場／族群 → 基本面與有來源的催化 → 日週K／籌碼 → 突破或回測觸發 → 扣成本 R:R。
8. 保存計畫狀態與不可回填的每日決策快照；新網站分別標出台美行情日期與產生時間。

## 判斷與時間設定

- 波段買點不以漲停、未漲停或當日漲幅作為條件。
- 突破與回測是兩種獨立型態，不用重複的均線訊號累加成勝率。
- 預計持有10～20個交易日（約2～4週）；有效訊號窗口為3個交易日。
- 觸發後第5個交易日重評。此處從**研究訊號**起算，不代表已收到實際成交回報。
- 延長到40交易日需要有效催化證據的 `extend_to_8_weeks: true` 且技術結構仍通過；不是虧損時自動延長。
- 現有計畫的進場、失效、目標及到期日凍結；同一型態到期後不得每天重設有效期。
- 目標使用既有歷史轉折高點，不能為了湊 R:R 製造目標。創新高而無合理歷史目標時待人工情境研究。
- 計算採用可設定的佣金、賣出稅與滑價假設。沒有真實即時買賣價差，不把收盤觸發稱為已驗證盤中可成交。
- 資料基準疑似因除權息／分割變動時，舊計畫轉為重評。

## 催化審核（尚未自動化）

**RSS 新聞標題不直接等於利多，也不會自動觸發買點。** 本版未接 AI，也未接具有公告時間的官方新聞全文分析。
FinMind 月營收 `create_time` 是供應商資料庫收錄日，不是公司公告時刻；財報 `date` 是期末日。
因此基本面可自動初篩，催化需要已查證的結構化研究證據。預設 `radar_evidence.json` 為空，
在填入有效證據前，候選股會標示「待確認」，**不會自行產生可執行進場建議**。

經查證後的格式（以下為格式範例，不是任何真實個股消息）：

```json
{
  "四碼代號": {
    "reviewed": true,
    "source_url": "https://來源的正式公告網址",
    "published_at": "2026-09-21T09:00:00+08:00",
    "observed_on": "2026-09-21",
    "valid_until": "2026-09-24",
    "thesis": "2～4週的研究理由",
    "impact": "新資訊如何影響營運預期",
    "priced_in_risk": "現價可能已反映多少、尚存的不確定性",
    "cancel_if": "哪些事實發生後撤銷原判斷",
    "extend_to_8_weeks": false
  }
}
```

`reviewed` 是人工查核標記，不是程式能驗證消息真假的保證。
此 repo 與網站是公開的，只放可公開的研究資料，勿放個人持倉、成本或密鑰。

## 排程、快取與通知

- 台股改為14:17初次報告、21:17補查較晚更新的籌碼；晚上優先複用同交易日有效行情，避免再全量逐檔下載。
- 美股維持收盤後獨立執行，設定台灣時間次日06:37；交易日曆處理美國假日與夏令時間。
- GitHub Actions 仍可能延遲，本版**不是盤中即時服務**。
- 台美報告、健康檢查共用寫入鎖，避免同時推送覆蓋彼此。
- 健康檢查偵測超過寬限期的行情過期；同平台排程全停時仍需外部監控，不能自我保證發現漏跑。
- Telegram 檢查 HTTP 與 JSON `ok`，限流可重試；固定報告按市場與行情日期去重。
- 發送前先把 outbox `sending` 狀態推回 repo，再發送、保存結果。網路中斷、程序終止導致 `sending/uncertain` 時，不盲目重送；需核對 Telegram 後人工修正狀態。
- 明確拒絕的 `failed` 可於下次執行補送。Telegram 沒有送訊息 idempotency key，因此不能同時承諾完全不漏送與絕不重複。
- `RADAR_MODE=live` 只啟用**收盤研究狀態通知**，並不新增即時行情或下單功能。

## 測試與使用

```sh
pip install -r requirements.txt
python -m unittest discover -s tests -v
python main.py --market tw --tw-limit 5
python notify.py --market tw
```

最後一行只預覽通知；只有明確加入 `--send` 才發送。小規模執行只寫 `.runtime/test_*`，
不覆蓋正式網站、追蹤資料或波段狀態。正式排程只能在 `master` 執行；分支與PR僅跑離線測試。

## 驗證限制與下一步

- `evaluation.replay` 提供次一交易日開盤成交、跳空、成本、同根K停損優先、時間出場，以及3／5／10／20／40交易日假設持有結果。
- 它是執行／結果評估工具，**尚未完成真實市場樣本外績效回測**。現有歷史焦點不足以還原完整五層策略。
- 使用現在查到的財報／還原行情回填過去，會產生前視或修訂偏差；本版先保存每日決策快照供前瞻驗證。
- 尚待串接：官方大盤／櫃買基準、可核對發布時刻的公告／新聞催化、即時報價與價差、盤中常駐執行環境、實際持倉／成交回報。
- 下一階段应先測出每筆扣成本期望值、最大回撤、訊號可成交性、族群／市場相對報酬與不同行情表現，再決定持有參數與是否打開 live。

資料欄位參考：
- https://finmind.github.io/tutor/TaiwanMarket/Fundamental/
- https://finmind.github.io/tutor/TaiwanMarket/Chip/
- https://finmind.github.io/tutor/TaiwanMarket/Technical/
- https://pypi.org/project/exchange_calendars/
